<h1>投稿一覧</h1>
 <p>ユーザー名:<%= @user.name %></p>
<% @posts.each do |post| %>
 
  <div>タイトル</div></br>
  <%= link_to post_path(post.id) do %>
    <%= post.title %>
  <% end %>
  <p>投稿本文:<%= post.body %></p>
  <p>投稿画像</p>
  <%= attachment_image_tag post, :post_image %>
<% end %>
